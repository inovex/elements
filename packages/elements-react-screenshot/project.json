{
  "name": "elements-react-screenshot",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/elements-react-screenshot/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nx/vite:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "outputPath": "dist/packages/elements-react-screenshot"
      },
      "configurations": {
        "development": {
          "mode": "development"
        },
        "production": {
          "mode": "production"
        }
      }
    },
    "serve": {
      "executor": "@nx/vite:dev-server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "elements-react-screenshot:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "elements-react-screenshot:build:development",
          "hmr": true
        },
        "production": {
          "buildTarget": "elements-react-screenshot:build:production",
          "hmr": false
        }
      }
    },
    "preview": {
      "executor": "@nx/vite:preview-server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "elements-react-screenshot:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "elements-react-screenshot:build:development"
        },
        "production": {
          "buildTarget": "elements-react-screenshot:build:production"
        }
      }
    },
    "test": {
      "executor": "@nx/vite:test",
      "outputs": ["{options.reportsDirectory}"],
      "options": {
        "passWithNoTests": true,
        "reportsDirectory": "../../coverage/packages/elements-react-screenshot"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": [
          "packages/elements-react-screenshot/**/*.{ts,tsx,js,jsx}"
        ]
      }
    }
  },
  "tags": []
}
