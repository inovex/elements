{
  "name": "elements-react",
  "sourceRoot": "packages/elements-react/src",
  "projectType": "library",
  "tags": [],
  "namedInputs": {
    "default": ["{projectRoot}/src/**/*", "{projectRoot}/../elements/dist/**/*"]
  },
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{projectRoot}/dist"],
      "options": {
        "cwd": "packages/elements-react",
        "command": "tsc -p .",
        "color": true
      }
    },
    "dev": {
      "executor": "nx:run-commands",
      "outputs": ["{projectRoot}/dist"],
      "options": {
        "cwd": "packages/elements-react",
        "command": "tsc -p .",
        "color": true
      }
    },
    "start": {
      "executor": "nx:run-commands",
      "outputs": ["{projectRoot}/dist"],
      "options": {
        "cwd": "packages/elements-react",
        "command": "nx watch --projects=elements-react -- nx run elements-react:dev"
      }
    },
    "clean": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "packages/elements-react",
        "command": "shx rm -rf dist"
      },
      "configurations": {
        "nodejs": {
          "command": "shx rm -rf node_modules"
        }
      }
    }
  }
}
